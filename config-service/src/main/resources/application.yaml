server:
  port: 8081
spring:
  cloud: 
    config:
      server:
        git:
          default-label: multiple-instances
          uri: https://github.com/skapazorro/microservicios-tutorial.git
          search-paths: config-data
  application:
    name: config-service
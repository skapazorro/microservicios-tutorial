server:
  port: 8001
eureka:
  client:
    fetch-registry: true
    register-whit-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
    instance: 
      hostname: localhost